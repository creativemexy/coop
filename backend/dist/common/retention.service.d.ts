import { ConfigService } from '@nestjs/config';
import { Repository } from 'typeorm';
import { LoginHistory } from '../modules/auth/entities/login-history.entity';
import { DeviceSession } from '../modules/auth/entities/device-session.entity';
import { UserActivity } from '../modules/users/entities/user-activity.entity';
import { AuditLog as BnplAuditLog } from '../modules/bnpl/entities/audit-log.entity';
import { SupportTicket } from '../modules/bnpl/entities/support-ticket.entity';
import { InAppNotification } from '../modules/notifications/entities/in-app-notification.entity';
import { SmsLog } from '../modules/sms/entities/sms-log.entity';
import { WebhookLog } from '../modules/payments/entities/webhook-log.entity';
import { KycSubmission } from '../modules/kyc/entities/kyc-submission.entity';
import { User } from '../modules/users/entities/user.entity';
import { AuditService } from './audit.service';
export declare class RetentionService {
    private readonly config;
    private readonly auditService;
    private readonly loginRepo;
    private readonly deviceRepo;
    private readonly activityRepo;
    private readonly auditRepo;
    private readonly ticketRepo;
    private readonly notifRepo;
    private readonly smsRepo;
    private readonly webhookRepo;
    private readonly kycRepo;
    private readonly userRepo;
    private readonly logger;
    private readonly defaults;
    constructor(config: ConfigService, auditService: AuditService, loginRepo: Repository<LoginHistory>, deviceRepo: Repository<DeviceSession>, activityRepo: Repository<UserActivity>, auditRepo: Repository<BnplAuditLog>, ticketRepo: Repository<SupportTicket>, notifRepo: Repository<InAppNotification>, smsRepo: Repository<SmsLog>, webhookRepo: Repository<WebhookLog>, kycRepo: Repository<KycSubmission>, userRepo: Repository<User>);
    private daysAgo;
    private retentionDays;
    purgeAll(): Promise<Record<string, number>>;
    anonymizeKycDocuments(): Promise<number>;
    anonymizeInactiveUsers(): Promise<number>;
}
